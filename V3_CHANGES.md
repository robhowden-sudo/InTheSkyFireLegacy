# v3 parity pass

- Preserves the larger Fire radar size requested by Rob.
- Weather history instruments now use bar charts throughout and retain the Windows-style dashboard split.
- On This Day history is scrollable instead of clipping.
- Sky View no longer shows a giant default Jupiter image before an object is selected.
- Launches now use Launch Library 2 first, cached LL2 data second, and RocketLaunch.Live's free next-five feed as a fallback when LL2 rate-limits (HTTP 429).
- Existing v2 Radar/Flight/Sky hand-offs and ISS cache behaviour retained.
